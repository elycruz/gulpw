/**
Conversion of http://eslint.org/docs/rules/ to an actual .eslintrc file:
Copied: 04/01/2015
Copied by: Ely De La Cruz <elycruz@elycruz.com>
*/

/**
Rules:
Rules in ESLint are divided into several categories to help you better understand their value. Additionally, not all rules are enabled by default. Those that are not enabled by default are marked as being off.
**-------------------------------------------------------------------------------------------------------------------**/
{
  /**
  Possible Errors:
  The following rules point out areas where you might have made mistakes.
  **-------------------------------------------------------------------------------------------------------------------**/

  "comma-dangle":         	false, //- disallow or enforce trailing commas
  "no-comma-dangle":        false, //- (deprecated) disallow trailing commas in object literals (off by default)
  "no-cond-assign":         true, //- disallow assignment in conditional expressions
  "no-console":         	true, //- disallow use of console (off by default in the node environment)
  "no-constant-condition":  true, //- disallow use of constant expressions in conditions
  "no-control-regex":       false, //- disallow control characters in regular expressions
  "no-debugger":         	false, //- disallow use of debugger
  "no-dupe-args":         	true, //- disallow duplicate arguments in functions
  "no-dupe-keys":         	true, //- disallow duplicate keys when creating object literals
  "no-duplicate-case":      true, //- disallow a duplicate case label.
  "no-empty":         		true, //- disallow empty statements
  "no-empty-class":         false, //- disallow the use of empty character classes in regular expressions
  "no-ex-assign":         	true, //- disallow assigning to the exception in a catch block
  "no-extra-boolean-cast":  true, //- disallow double-negation boolean casts in a boolean context
  "no-extra-parens":        false, //- disallow unnecessary parentheses (off by default)
  "no-extra-semi":         	true, //- disallow unnecessary semicolons
  "no-func-assign":         true, //- disallow overwriting functions written as function declarations
  "no-inner-declarations":  false, //- disallow function or variable declarations in nested blocks
  "no-invalid-regexp":      true, //- disallow invalid regular expression strings in the RegExp constructor
  "no-irregular-whitespace":false, //- disallow irregular whitespace outside of strings and comments
  "no-negated-in-lhs":    	true, //- disallow negation of the left operand of an in expression
  "no-obj-calls":         	false, //- disallow the use of object properties of the global object (Math and JSON) as functions
  "no-regex-spaces":      	true, //- disallow multiple spaces in a regular expression literal
  "no-reserved-keys":     	false, //- disallow reserved words being used as object literal keys (off by default)
  "no-sparse-arrays":     	false, //- disallow sparse arrays
  "no-unreachable":       	true, //- disallow unreachable statements after a return, throw, continue, or break statement
  "use-isnan":         		false, //- disallow comparisons with the value NaN
  "valid-jsdoc":         	false, //- Ensure JSDoc comments are valid (off by default)
  "valid-typeof":         	true, //- Ensure that the results of typeof are compared against a valid string

  /**
  Best Practices:
  These are rules designed to prevent you from making mistakes. They either prescribe a better way of doing something or help you avoid footguns.
  **-------------------------------------------------------------------------------------------------------------------**/

  "block-scoped-var":       false, //- treat var statements as if they were block scoped (off by default)
  "complexity":         	false, //- specify the maximum cyclomatic complexity allowed in a program (off by default)
  "consistent-return":      false, //- require return statements to either always or never specify values
  "curly":         			true, //- specify curly brace conventions for all control statements
  "default-case":         	true, //- require default case in switch statements (off by default)
  "dot-notation":         	true, //- encourages use of dot notation whenever possible
  "eqeqeq":         		true, //- require the use of === and !==
  "guard-for-in":         	true, //- make sure for-in loops have an if statement (off by default)
  "no-alert":         		false, //- disallow the use of alert, confirm, and prompt
  "no-caller":         		true, //- disallow use of arguments.caller or arguments.callee
  "no-div-regex":         	false, //- disallow division operators explicitly at beginning of regular expression (off by default)
  "no-else-return":         false, //- disallow else after a return in an if (off by default)
  "no-empty-label":         false, //- disallow use of labels for anything other then loops and switches
  "no-eq-null":         	false, //- disallow comparisons to null without a type-checking operator (off by default)
  "no-eval":         		false, //- disallow use of eval()
  "no-extend-native":       true, //- disallow adding to native types
  "no-extra-bind":         	false, //- disallow unnecessary function binding
  "no-fallthrough":         true, //- disallow fallthrough of case statements
  "no-floating-decimal":    true, //- disallow the use of leading or trailing decimal points in numeric literals (off by default)
  "no-implied-eval":        true, //- disallow use of eval()-like methods
  "no-iterator":            true, //- disallow usage of __iterator__ property
  "no-labels":              true, //- disallow use of labeled statements
  "no-lone-blocks":         true, //- disallow unnecessary nested blocks
  "no-loop-func":           true, //- disallow creation of functions within loops
  "no-multi-spaces":        false, //- disallow use of multiple spaces
  "no-multi-str":           true, //- disallow use of multiline strings
  "no-native-reassign":     true, //- disallow reassignments of native objects
  "no-new":                 true, //- disallow use of new operator when not part of the assignment or comparison
  "no-new-func":            true, //- disallow use of new operator for Function object
  "no-new-wrappers":        true, //- disallows creating new instances of String,Number, and Boolean
  "no-octal":               false, //- disallow use of octal literals
  "no-octal-escape":        false, //- disallow use of octal escape sequences in string literals, such as var foo = "Copyright \251";
  "no-param-reassign":      false, //- disallow reassignment of function parameters (off by default)
  "no-process-env":         false, //- disallow use of process.env (off by default)
  "no-proto":               true, //- disallow usage of __proto__ property
  "no-redeclare":           true, //- disallow declaring the same variable more then once
  "no-return-assign":       true, //- disallow use of assignment in return statement
  "no-script-url":          false, //- disallow use of javascript: urls.
  "no-self-compare":        true, //- disallow comparisons where both sides are exactly the same (off by default)
  "no-sequences":           true, //- disallow use of comma operator
  "no-throw-literal":       false, //- restrict what can be thrown as an exception (off by default)
  "no-unused-expressions":  false, //- disallow usage of expressions in statement position
  "no-void":                false, //- disallow use of void operator (off by default)
  "no-warning-comments":    false, //- disallow usage of configurable warning terms in comments - e.g. TODO or FIXME (off by default)
  "no-with":                true, //- disallow use of the with statement
  "radix":                  true, //- require use of the second argument for parseInt() (off by default)
  "vars-on-top":            true, //- requires to declare all vars on top of their containing scope (off by default)
  "wrap-iife":              true, //- require immediate function invocation to be wrapped in parentheses (off by default)
  "yoda":                   false, //- require or disallow Yoda conditions

  /**
  Strict Mode:
  These rules relate to using strict mode.
  **-------------------------------------------------------------------------------------------------------------------**/

  "global-strict":          false, //- (deprecated) require or disallow the "use strict" pragma in the global scope (off by default in the node environment)
  "no-extra-strict":        false, //- (deprecated) disallow unnecessary use of "use strict"; when already in strict mode
  "strict":                 true, //- controls location of Use Strict Directives

  /**
  Variables:
  These rules have to do with variable declarations.
  **-------------------------------------------------------------------------------------------------------------------**/

  "no-catch-shadow":        true, //- disallow the catch clause parameter name being the same as a variable in the outer scope (off by default in the node environment)
  "no-delete-var":          false, //- disallow deletion of variables
  "no-label-var":           true, //- disallow labels that share a name with a variable
  "no-shadow":              true, //- disallow declaration of variables already declared in the outer scope
  "no-shadow-restricted-names": true, //- disallow shadowing of names such as arguments
  "no-undef":               true, //- disallow use of undeclared variables unless mentioned in a /*global */ block
  "no-undef-init":          true, //- disallow use of undefined when initializing variables
  "no-undefined":           false, //- disallow use of undefined variable (off by default)
  "no-unused-vars":         true, //- disallow declaration of variables that are not used in the code
  "no-use-before-define":   false, //- disallow use of variables before they are defined

  /**
  Node.js:
  These rules are specific to JavaScript running on Node.js.
  **-------------------------------------------------------------------------------------------------------------------**/

  "handle-callback-err":    false, //- enforces error handling in callbacks (off by default) (on by default in the node environment)
  "no-mixed-requires":      false, //- disallow mixing regular variable and require declarations (off by default) (on by default in the node environment)
  "no-new-require":         true, //- disallow use of new operator with the require function (off by default) (on by default in the node environment)
  "no-path-concat":         false, //- disallow string concatenation with __dirname and __filename (off by default) (on by default in the node environment)
  "no-process-exit":        true, //- disallow process.exit() (on by default in the node environment)
  "no-restricted-modules":  false, //- restrict usage of specified node modules (off by default)
  "no-sync":         		false, //- disallow use of synchronous methods (off by default)

  /**
  Stylistic Issues:
  These rules are purely matters of style and are quite subjective.
  **-------------------------------------------------------------------------------------------------------------------**/

  "indent":                 4, //- this option sets a specific tab width for your code (off by default)
  "brace-style":            false, //- enforce one true brace style (off by default)
  "camelcase":              false, //- require camel case names
  "comma-spacing":          false, //- enforce spacing before and after comma
  "comma-style":            false, //- enforce one true comma style (off by default)
  "consistent-this":        false, //- enforces consistent naming when capturing the current execution context (off by default)
  "eol-last":               true, //- enforce newline at the end of file, with no multiple empty lines
  "func-names":             false, //- require function expressions to have a name (off by default)
  "func-style":             false, //- enforces use of function declarations or expressions (off by default)
  "key-spacing":            true, //- enforces spacing between keys and values in object literal properties
  "max-nested-callbacks":   3, //- specify the maximum depth callbacks can be nested (off by default)
  "new-cap":                true, //- require a capital letter for constructors
  "new-parens":             true, //- disallow the omission of parentheses when invoking a constructor with no arguments
  "newline-after-var":      true, //- allow/disallow an empty newline after var statement (off by default)
  "no-array-constructor":   true, //- disallow use of the Array constructor
  "no-inline-comments":     false, //- disallow comments inline after code (off by default)
  "no-lonely-if":           true, //- disallow if as the only statement in an else block (off by default)
  "no-mixed-spaces-and-tabs":   true, //- disallow mixed spaces and tabs for indentation
  "no-multiple-empty-lines":    true, //- disallow multiple empty lines (off by default)
  "no-nested-ternary":          true, //- disallow nested ternary expressions (off by default)
  "no-new-object":              true, //- disallow use of the Object constructor
  "no-space-before-semi":       true, //- (deprecated) disallow space before semicolon (off by default)
  "no-spaced-func":             false, //- disallow space between function identifier and application
  "no-ternary":                 false, //- disallow the use of ternary operators (off by default)
  "no-trailing-spaces":         false, //- disallow trailing whitespace at the end of lines
  "no-underscore-dangle":       false, //- disallow dangling underscores in identifiers
  "no-wrap-func":               true, //- disallow wrapping of non-IIFE statements in parens
  "one-var":                    true, //- allow just one var statement per function (off by default)
  "operator-assignment":        false, //- require assignment operator shorthand where possible or prohibit it entirely (off by default)
  "padded-blocks":              false, //- enforce padding within blocks (off by default)
  "quote-props":                false, //- require quotes around object literal property names (off by default)
  "quotes":                     "single", //- specify whether double or single quotes should be used
  "semi":                       true, //- require or disallow use of semicolons instead of ASI
  "semi-spacing":               false, //- enforce spacing before and after semicolons
  "sort-vars":                  false, //- sort variables within the same declaration block (off by default)
  "space-after-function-name":        true, //- (deprecated) require a space after function names (off by default)
  "space-after-keywords":             true, //- require a space after certain keywords (off by default)
  "space-before-blocks":              true, //- require or disallow space before blocks (off by default)
  "space-before-function-paren":      false, //- require or disallow space before function opening parenthesis (off by default)
  "space-before-function-parentheses":false, //- (deprecated) require or disallow space before function parentheses (off by default)
  "space-in-brackets":                false, //- require or disallow spaces inside brackets (off by default)
  "space-in-parens":                  false, //- require or disallow spaces inside parentheses (off by default)
  "space-infix-ops":                  false, //- require spaces around operators
  "space-return-throw-case":          true, //- require a space after return, throw, and case
  "space-unary-ops":                  true, //- Require or disallow spaces before/after unary operators (words on by default, nonwords off by default)
  "space-unary-word-ops":             false, //- (deprecated) Require or disallow spaces before/after unary operators (words on by default, nonwords off by default)
  "spaced-line-comment":              false, //- require or disallow a space immediately following the // in a line comment (off by default)
  "wrap-regex":                       false, //- require regex literals to be wrapped in parentheses (off by default)

  /**
  ECMAScript 6:
  These rules are only relevant to ES6 environments and are off by default.
  **-------------------------------------------------------------------------------------------------------------------**/

  "no-var":                 false, //- require let or const instead of var (off by default)
  "generator-star":         false, //- (deprecated) enforce the position of the * in generator functions (off by default)
  "generator-star-spacing": false, //- enforce the spacing around the * in generator functions (off by default)

  /**
  Legacy:
  The following rules are included for compatibility with JSHint and JSLint. While the names of the rules may not match up with the JSHint/JSLint counterpart, the functionality is the same.
  **-------------------------------------------------------------------------------------------------------------------**/

  "max-depth":              3, //- specify the maximum depth that blocks can be nested (off by default)
  "max-len":                120, //- specify the maximum length of a line in your program (off by default)
  "max-params":             5, //- limits the number of parameters that can be used in the function declaration. (off by default)
  "max-statements":         false, //- specify the maximum number of statement allowed in a function (off by default)
  "no-bitwise":             true, //- disallow use of bitwise operators (off by default)
  "no-plusplus":            true //- disallow use of unary operators, ++ and -- (off by default)
}