### requirejs
RequireJs task.

#####In 'bundle.wrangler.config.yaml':
```
tasks:
  requirejs: null # requirejs options here
    #options:
      ...
```

##### In {bundle}.*:
```
requirejs:
  options:
    ...
```
